# Node.js
node_modules/
npm-debug.log
yarn-error.log

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Coverage + logs (backend/frontend) — never commit
coverage/
backend/coverage/
frontend/coverage/
logs/
backend/logs/
*.log
test-results/
playwright-report/

# Database files
*.db
*.db-journal
*.sqlite

# Docs are versioned as a whole — no blanket ignore. (A previous rule ignored
# docs/* and silently kept 9 of 16 docs out of the repo.)

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDEs and Editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# graphify knowledge graph (AST-extracted, rebuilt on demand — exclude to keep prompt cache clean)
graphify-out/
